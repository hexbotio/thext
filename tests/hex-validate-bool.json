{
  "rule": "Validation Test",
  "match": "validate*",
  "actions": [
    {
      "type": "hex-validate",
      "command": "${hex.input.1}",
      "config": {
        "type": "bool",
        "success": "Success",
        "failure": "Failure"
      }
    }
  ]
}
